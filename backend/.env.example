APP_ENV=development
PORT=8080
# When running via docker-compose the host is the "postgres" service name.
DATABASE_URL=postgres://casinha:casinha@postgres:5432/casinha?sslmode=disable
# Reserved for Phase 6 auth.
JWT_SECRET=change-me-in-production
